<!-- AUTO-GENERATED by tests/llm/test_structured_recovery_matrix_report.py -->

刷新：`cd Cuti-VideoAgent && PYTHONPATH=. pytest tests/llm/test_structured_recovery_matrix_report.py -v`（需 `OPENAI_API_KEY`）。

# Structured recovery matrix (Tool vs Provider) — 真实 API 摘录

| Case | Strategy | Status | Notes |
|------|----------|--------|-------|
| tool_ingraph_invalid_args_then_llm_fixes | tool | passed | 图内纠错：共 6 条消息；含 Error 前缀的 ToolMessage 条数=1（LangChain 注入的请修复提示）。 |
| tool_happy_path_real_only | tool | passed | 单次或图内短路径成功（无人工坏 tool） |
| provider_first_bad_json_then_execute_with_resilience_retry | provider | passed | 首轮 patch 注入非 JSON → StructuredOutputValidationError；execute_with_resilience 同路由第二次调用 invoke_fn（新 LLM、无 patch）成功。 |
| provider_happy_path_real_only | provider | passed | Provider 原生结构化一次成功 |
| execute_with_resilience_transient_timeout_same_route_retry | provider | passed | invoke_fn 调用次数=2（1 次模拟超时 + 1 次真实成功） |

## tool_ingraph_invalid_args_then_llm_fixes (tool)

- **status**: passed
- **structured**: `TinyAnswer(answer='ok')`

图内纠错：共 6 条消息；含 Error 前缀的 ToolMessage 条数=1（LangChain 注入的请修复提示）。

**Messages (chronological):**

```
[0] system | content='You use the structured output tool. If you see a tool error, fix and call the tool again. Final field answer must be exactly: ok'
[1] human | content='go'
[2] ai | tool_call:TinyAnswer args={"answer": 42}
[3] tool | content="Error: Failed to parse structured output for tool 'TinyAnswer': Failed to parse data to TinyAnswer: 1 validation error for TinyAnswer answer   Input should be a valid string [type=string_type, input_value=42, input_type="
[4] ai | tool_call:TinyAnswer args={"answer": "ok"}
[5] tool | content="Returning structured response: answer='ok'"
```

## tool_happy_path_real_only (tool)

- **status**: passed
- **structured**: `TinyAnswer(answer='ok')`

单次或图内短路径成功（无人工坏 tool）

**Messages (chronological):**

```
[0] system | content='Call structured tool once. Field answer must be exactly: ok'
[1] human | content='ping'
[2] ai | tool_call:TinyAnswer args={"answer": "ok"}
[3] tool | content="Returning structured response: answer='ok'"
```

## provider_first_bad_json_then_execute_with_resilience_retry (provider)

- **status**: passed
- **structured**: `TinyAnswer(answer='ok')`

首轮 patch 注入非 JSON → StructuredOutputValidationError；execute_with_resilience 同路由第二次调用 invoke_fn（新 LLM、无 patch）成功。

**Messages (chronological):**

```
--- invoke_pass=1 FAILED: StructuredOutputValidationError: Failed to parse structured output for tool 'TinyAnswer': Native structured output expected valid JSON for TinyAnswer, but parsing failed: Expecting value: line 1 column 1 (char 0)..
--- invoke_pass=2 ---
[0] system | content='Native JSON output per schema. Field answer must be exactly: ok'
[1] human | content='ping'
[2] ai | content='{"answer":"ok"}'
--- final merged trace ---
[0] system | content='Native JSON output per schema. Field answer must be exactly: ok'
[1] human | content='ping'
[2] ai | content='{"answer":"ok"}'
```

## provider_happy_path_real_only (provider)

- **status**: passed
- **structured**: `TinyAnswer(answer='ok')`

Provider 原生结构化一次成功

**Messages (chronological):**

```
[0] system | content='JSON per schema. Field answer must be exactly: ok'
[1] human | content='ping'
[2] ai | content='{"answer":"ok"}'
```

## execute_with_resilience_transient_timeout_same_route_retry (provider)

- **status**: passed
- **structured**: `TinyAnswer(answer='ok')`

invoke_fn 调用次数=2（1 次模拟超时 + 1 次真实成功）

**Messages (chronological):**

```
invoke_fn_calls=2
TinyAnswer(answer='ok')
```
