# Filtered OpenMontage skills

These instruction files were moved out of the active skill surface because they
depend on runtimes or APIs Cuti does not ship (Remotion, WhisperX, avatar GPU,
ComfyUI, stock APIs, podcast/avatar pipelines, etc.).

See `../COMPAT.md` for the full matrix and reasons.
