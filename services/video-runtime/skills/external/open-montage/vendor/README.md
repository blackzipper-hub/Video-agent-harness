# Vendored OpenMontage tool sources (reference only)

These files document upstream tool contracts. They are **not** executed by Cuti.
Runtime invocation goes through `open_montage.tool.invoke` / `scripts/om_tools.py`
which bridge to Media + provider.generate. See ../COMPAT.md.
