# Pipeline skills (LangGraph stage deep agents)

Layout: see `docs/stage-artifact-layout.md`.

```
skills/stages/<stage>/<skill-name>/SKILL.md
skills/creative/<name>/SKILL.md   # shared cinematic craft (storytelling, short-form, …)
```

Narrative stages (analysis / outline / script / scene) load `stages/<stage>` **and** `creative/`.

Companion edit skills remain under `app/services/agent/video_edit/skills/` for now.
