"""
app.contracts — Python source of truth for LLM / Artifact / DB shapes.

Agent-readable JSON Schema + skills live under ``kit/`` (see docs/stage-artifact-layout.md).
Regenerate JSON: ``python -m app.contracts.export_json_schemas``.
"""
