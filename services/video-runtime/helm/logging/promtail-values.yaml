# 与 Loki 同 namespace `monitoring`、release 名 `loki` 时，短名 `loki-gateway` 即可。

config:
  clients:
    - url: http://loki-gateway/loki/api/v1/push

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
