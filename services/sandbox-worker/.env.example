SANDBOX_ENVIRONMENT=development
SANDBOX_INTERNAL_TOKEN=change-me
SANDBOX_STAGING_ROOT=/tmp/cuti-sandbox

# DEVELOPMENT ONLY: executes Skill code as a local subprocess with inherited
# environment, filesystem permissions, and network access. Production rejects it.
SANDBOX_UNSAFE_DEV_MODE=false

