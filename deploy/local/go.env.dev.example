# Copy to deploy/local/go-env/.env.dev  (that path is gitignored)
# JWT must be >= 32 chars. Stripe can be a test key; login still needs a real mail/OTP path if you exercise it.

JWT_SECRET_KEY=local-dev-jwt-secret-key-32chars-min
STRIPE_SECRET_KEY=sk_test_local_not_for_production
STRIPE_WEBHOOK_SECRET=whsec_local_not_for_production
